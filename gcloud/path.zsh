
# The next line updates PATH for the Google Cloud SDK.
if [ -f '/Users/cam/google-cloud-sdk/path.zsh.inc' ]; then . '/Users/cam/google-cloud-sdk/path.zsh.inc'; fi

if [ -f "$DOTFILES/gcloud/path.zsh.inc" ]; then . "$DOTFILES/gcloud/path.zsh.inc"; fi

# The next line enables shell command completion for gcloud.
if [ -f '/Users/cam/google-cloud-sdk/completion.zsh.inc' ]; then . '/Users/cam/google-cloud-sdk/completion.zsh.inc'; fi